display = frame.copy()

# AI FRAME (separate from preview)
if use_oak:
    ai_frame = oak_full_queue.get().getCvFrame()
else:
    ai_frame = frame

try:
    patches = extract_features(ai_frame)
    heatmap = build_heatmap(ai_frame, patches, anomaly_memory)
