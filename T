else:
    use_oak = False

    cap = cv2.VideoCapture(cam[1], cv2.CAP_DSHOW)  # important on Windows

    if not cap.isOpened():
        messagebox.showerror("Error", f"Camera {cam[1]} not detected")
        cap = None
        return

    status_label.config(text=f"Camera {cam[1]} selected")
