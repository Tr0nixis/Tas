disp_h, disp_w = display.shape[:2]

font_scale = max(0.6, disp_w / 600)
thickness = max(2, int(font_scale * 2))

cv2.putText(
    display,
    f"{label} {conf:.2f}",
    (10, int(40 * font_scale)),
    cv2.FONT_HERSHEY_SIMPLEX,
    font_scale,
    color,
    thickness
)
