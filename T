# =========================================================
# LOAD DATABASE
# =========================================================

def clear_valid_folder():

    for file in os.listdir(VALID_DIR):

        try:
            os.remove(
                os.path.join(
                    VALID_DIR,
                    file
                )
            )
        except:
            pass

    status_label.config(
        text="Training folder cleared"
    )


def load_database():
