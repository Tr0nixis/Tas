# ---------------- ROI ----------------
if bbox is not None:
    x1, y1, x2, y2 = bbox

    x1 = max(0, x1)
    y1 = max(0, y1)
    x2 = min(frame.shape[1], x2)
    y2 = min(frame.shape[0], y2)

    roi = frame[y1:y2, x1:x2]

    if roi.size > 0:
        frame = roi
