np.save(os.path.join(MEMORY_DIR, f"{name}.npy"), np.array(anomaly_memory))

refresh_memory_list()

# Auto-select the newly trained model
memory_list.selection_clear(0, tk.END)

for i in range(memory_list.size()):
    if memory_list.get(i) == f"{name}.npy":
        memory_list.selection_set(i)
        memory_list.activate(i)
        memory_list.see(i)
        break

# Auto-load it
load_selected_memory()

status_label.config(text=f"Memory '{name}' trained and loaded")
