def release(e):
    global bbox

    x1 = state["x1"]
    y1 = state["y1"]

    x2 = e.x
    y2 = e.y

    # clamp safety
    x1, x2 = sorted([x1, x2])
    y1, y2 = sorted([y1, y2])

    bbox = (x1, y1, x2, y2)

    status_label.config(text="ROI set (fixed)")
    win.destroy()
