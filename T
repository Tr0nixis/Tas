# ---------------- GET FRAME ----------------
if use_oak:
    frame = oak_preview_queue.get().getCvFrame()
else:
    if cap is None:
        return

    ret, frame = cap.read()
    if not ret:
        root.after(10, update_frame)
        return
