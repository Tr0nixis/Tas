patches = extract_features(frame)

heatmap = build_heatmap(
    frame,
    patches,
    anomaly_memory
)

if heatmap is not None:
    output = cv2.addWeighted(
        output,
        0.65,
        heatmap,
        0.35,
        0
    )
